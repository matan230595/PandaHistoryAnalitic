# Required because vite-plugin-pwa@1.x declares peer dep on vite <=7
# while we use vite@8. The flag tells npm to use legacy resolution
# (same as npm < 7 behaviour) instead of failing on the conflict.
legacy-peer-deps=true
