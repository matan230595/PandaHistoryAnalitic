# Password protection (client-side)
# NOTE: This is a basic local gate (not a secure backend auth).
# Set it in Vercel as an Environment Variable: VITE_APP_PASSWORD
VITE_APP_PASSWORD=panda

# Optional: app title
VITE_APP_TITLE=PandaHistoryAnalitic
